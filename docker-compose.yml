version: "3.8"

services:
  # === PROD BACKEND ===
  backend-prod:
    image: upload-be-prod:${APP_IMAGE_TAG:-latest}
    build:
      context: ./server
      dockerfile: Dockerfile
      args:
        NODE_ENV: production
    container_name: upload-be-prod
    ports:
      - "3400:3400"
    env_file:
      - ./server/.env.production  # Docker ще зареди променливите автоматично
    volumes:
    - /app/node_modules
    - ./server/.env.production:/app/.env.production
    - /ssd/docker/mitaka-website/mitaka-website/upload:/app/upload
    restart: always

  # === PROD FRONTEND ===
  frontend-prod:
    image: upload-fe-prod:${APP_IMAGE_TAG:-latest}
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NODE_ENV: production
    container_name: upload-fe-prod
    env_file:
      - .env.production
    ports:
      - "3500:80"
    depends_on:
      - backend-prod
    environment:
      - NODE_ENV=production
    volumes:
      - /ssd/docker/mitaka-website/mitaka-website/upload:/app/upload
      - /ssd/docker/share_upload_images:/app/upload
    restart: always

networks:
  default:
    external:
      name: docker_network
